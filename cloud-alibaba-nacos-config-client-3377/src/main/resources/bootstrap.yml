server:
  port: 3377

spring:
  application:
    name: cloud-alibaba-nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml  #指定yaml格式的配置
        group: DEFAULT_GROUP  #在此处选择配置中心的分组
#        namespace: b5e91798-8f35-4306-9aff-265cb13d57fd   #选择命名空间,Nacos2的版本无法新建命名空间